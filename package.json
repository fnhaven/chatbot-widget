{
  "name": "chatbot-widget",
  "version": "1.0.0",
  "description": "Embeddable chatbot widget (Vanilla JS, Shadow DOM)",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "terser": "^5.34.0"
  }
}
